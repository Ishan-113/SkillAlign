# MongoDB Atlas
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/sih134?retryWrites=true&w=majority
MONGO_DB_NAME=sih134
API_PORT=8000

# ---------------------------------------------------------------------------
# Job ingestion - Adzuna Jobs API (https://developer.adzuna.com/)
# Leave blank to fall back to generated/mock data until a key is obtained.
# These are read from environment / GitHub Actions secrets. Never hardcode.
# ---------------------------------------------------------------------------
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
ADZUNA_COUNTRY=in
ADZUNA_BASE_URL=https://api.adzuna.com/v1/api/jobs
ADZUNA_MAX_PAGES=10
ADZUNA_RESULTS_PER_PAGE=50
ADZUNA_REQUEST_DELAY=1.0

# ---------------------------------------------------------------------------
# Adzuna free-tier quota budgeting.
# Defaults match Adzuna's published free limits: 25/min, 250/day, 1000/week,
# 2500/month. Each paged search is one "hit", so a run capping at ADZUNA_MAX_PAGES
# pages uses that many hits. The ingestion caps pages per run to stay within these
# budgets and stops gracefully (partial run) rather than failing the cron if the
# daily/weekly/monthly cap would be exceeded.
# ---------------------------------------------------------------------------
ADZUNA_QUOTA_DAY_HITS=250
ADZUNA_QUOTA_WEEK_HITS=1000
ADZUNA_QUOTA_MONTH_HITS=2500
ADZUNA_QUOTA_SAFETY_MARGIN=10

# When true (default), ingestion uses generated mock data when no valid job API
# key is configured so the app remains functional for development/demos.
ENABLE_MOCK_FALLBACK=true

# ---------------------------------------------------------------------------
# Scheduling / freshness
# ---------------------------------------------------------------------------
JOBS_UPDATE_INTERVAL_HOURS=6
CURRICULUM_UPDATE_INTERVAL_DAYS=30
STALE_JOB_DAYS=30
